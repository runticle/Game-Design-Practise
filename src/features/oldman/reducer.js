const initialState = {
  position: [120,120],
}

const oldmanReducer = (state = initialState, action ) => {
  switch(action.type) {
    default:
      return state
  }
}

export default oldmanReducer
